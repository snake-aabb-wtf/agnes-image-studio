# Agnes Image Studio 依赖清单
# 安装：python -m pip install -r requirements.txt

PySide6>=6.6,<7       # Qt6 GUI 框架
httpx>=0.27           # 现代 HTTP 客户端
Pillow>=10            # 图片处理（缩略图、Data URI 编解码）
qtawesome>=1.3        # FontAwesome 图标
platformdirs>=4       # 跨平台用户数据目录
